[build]
  base    = "client"
  command = "npm install && npm run build"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "24"

# SPA fallback — all routes go to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
