[build]
  base = "client"
  command = "npm install && npm run generate"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — all routes go to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
